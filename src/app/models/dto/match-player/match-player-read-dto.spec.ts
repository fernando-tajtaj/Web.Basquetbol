import { MatchPlayerReadDto } from './match-player-read-dto';

describe('MatchPlayerReadDto', () => {
  it('should create an instance', () => {
    expect(new MatchPlayerReadDto()).toBeTruthy();
  });
});
