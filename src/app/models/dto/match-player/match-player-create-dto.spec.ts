import { MatchPlayerCreateDto } from './match-player-create-dto';

describe('MatchPlayerCreateDto', () => {
  it('should create an instance', () => {
    expect(new MatchPlayerCreateDto()).toBeTruthy();
  });
});
