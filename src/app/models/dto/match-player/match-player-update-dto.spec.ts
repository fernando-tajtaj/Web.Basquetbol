import { MatchPlayerUpdateDto } from './match-player-update-dto';

describe('MatchPlayerUpdateDto', () => {
  it('should create an instance', () => {
    expect(new MatchPlayerUpdateDto()).toBeTruthy();
  });
});
