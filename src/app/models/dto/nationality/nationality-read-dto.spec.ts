import { NationalityReadDto } from './nationality-read-dto';

describe('NationalityReadDto', () => {
  it('should create an instance', () => {
    expect(new NationalityReadDto()).toBeTruthy();
  });
});
